package v2v.simulations.v2v;

import org.car2x.veins.nodes.Scenario;
import org.car2x.veins.nodes.Car;  // Import Car module

network V2VNetwork extends Scenario
{
    parameters:
        int numVehicles = default(10);

    submodules:

}